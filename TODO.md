# TODO

- [ ] Inspect current `assets/css/style.css` for responsive gaps/duplicates affecting `index.html` (hero, cards, gallery).
- [ ] Update `assets/css/style.css` with targeted responsive rules for hero/video/content and gallery overlays.
- [ ] Fix duplicated/conflicting `@keyframes floatingCard` and consolidate mobile media rules if needed.
- [ ] Add/adjust mobile scaling rules for cards/typography to prevent clipping and horizontal overflow.
- [ ] Validate responsiveness by running the site and checking common breakpoints (320/375/768/1024).

